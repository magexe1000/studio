# Rollback Baseline: v3.6.81 Stable Before Cleanup

This document preserves the baseline metadata and instructions for rolling back to the stable `v3.6.81` release before any cleanup or optimization changes are applied.

---

## Metadata

* **Stable Release Version**: `v3.6.81`
* **Version Code**: `108`
* **Commit Hash**: `32391a11be82afcae238d7e275d3ce4d1a36b543`
* **Backup Tag**: `backup/v3.6.81-stable-before-cleanup`
* **Release URL**: [GitHub Release v3.6.81](https://github.com/MAGEXE1000/Studio/releases/tag/v3.6.81)
* **APK Download URL**: [studio-3.6.81.apk](https://github.com/MAGEXE1000/Studio/releases/download/v3.6.81/studio-3.6.81.apk)
* **APK SHA**: *Not computed locally (built & signed remotely on the release pipeline).*
* **Firebase Metadata State**: `version.json` maps `version` to `"3.6.81"`, with 5 release notes bullets.

---

## Reason for Backup

To establish a verified rollback point before performing codebase cleanups, duplicate system consolidations, bundle size reductions, and build/update optimizations. This ensures we can recover to the exact state of the `v3.6.81` release in case of regression.

---

## How to Roll Back

To restore the codebase to the exact state of the `v3.6.81` release:

1. **Fetch and checkout the backup tag:**
   ```bash
   git fetch origin --tags
   git checkout backup/v3.6.81-stable-before-cleanup
   ```
2. **(Optional) Recreate a release branch from this tag:**
   ```bash
   git checkout -b release/v3.6.81-rollback backup/v3.6.81-stable-before-cleanup
   ```
