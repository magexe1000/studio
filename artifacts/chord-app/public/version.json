{
  "version": "3.6.21",
  "changelog": "• Hardened release pipeline push phase with retry logic and exponential backoff to handle transient remote push conflicts.",
  "releaseNotes": {
    "fixed": [
      "Hardened release pipeline push phase with retry logic and exponential backoff to handle transient remote push conflicts."
    ]
  },
  "mandatory": false
}
