/**
 * Studio Admin Authorization List (UID-based)
 *
 * Place your unique Firebase Auth UID in this array to grant absolute Admin bypass.
 * Admins bypass all billing restrictions, can test all Core and Pro features,
 * and automatically receive the golden-orange Admin badge.
 *
 * Do NOT use email checks or localStorage.
 */
export const adminUIDs: string[] = [
  // Add your UID here to automatically become admin
];
